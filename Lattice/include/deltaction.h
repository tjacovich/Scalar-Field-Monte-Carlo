#ifndef _deltaction_h   /* Include guard */
#define _deltaction_h

 void deltaction(double* phi, double temp,  double a, double* S_n, double* S_o, int dim_lat, int dim_y, int dim_z, int dim_t, int cnt, double lambda, double h, double* phiI, double tempI);

#endif //Lattice Monte Carlo
